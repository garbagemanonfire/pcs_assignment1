var productName;
var productCount;
var currentPrice;
var totalCost;
var productTax;
var cashierSays

productName = "cookies";
productCount = 2;
currentPrice = 3;
productTax = productCount * currentPrice * .07;
totalCost = currentPrice + productTax;
cashierSays = "Your " + productName + " cost $" + totalCost;
console.log(cashierSays);

// create a javascript file (variables.js) that 
// creates some variables 
// at least 1 string, and a number,
// assigns some values, 
// creates a complex expression, 
// runs an operation, 
// give output via console.log (i.e. console.log FullName), 
// put it into PCS_Assignment_1 repo

/*
dlmanning comments:

Terrific work! I like that you tied to requirements together into a cohesive theme.

*/